const { staffService } = require('../../services');
const { HRApprovalMail } = require('../../utils/mail');

async function HR_Approval(req, res, next) {
    try {
        const { email } = req.body;
    }
}