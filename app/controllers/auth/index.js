const login = require('./login');
const forgot_pwd = require('./forgot-password');
const resetPassword = require('./reset-password');

module.exports = {
    login,
    forgot_pwd,
    resetPassword
}